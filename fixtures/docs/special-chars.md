# 価格は[要確認]です

キーワードに正規表現の特殊文字(`[`, `]`など)が含まれる場合の、リテラル一致確認用ファイルです。`-Keyword`に`[要確認]`を指定すると、この見出しがヒットする想定です(正規表現パターンとして解釈されてはいけません)。

## a.b.cの表記について

`-Keyword`に`a.b`を指定すると、この見出しがヒットする想定です(`.`が任意の1文字を表す正規表現として解釈されてはいけません)。
